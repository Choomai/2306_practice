var f:text;
n,i,j,k,cnt:longint;
map:array[1..100] of byte;
begin // Chua hoan thanh
  // assign(f,'117.inp');
  // reset(f);
  // read(f,n);
  // close(f);
  // assign(f,'117.out');
  // rewrite(f);
  // if n>2 then 
  // begin 
  //   writeln(f,1,':',1);
  //   writeln(f,2,':',2);
  // end;
  // for i:=3 to n do
  // begin
  //   cnt:=1;
  //   for j:=1 to i map[j]:=1;
  //   for j:=1 to i do
  //     if (map[j]=1) and (map[j+1]=1) then 
  //     begin
  //       map[j]:=2;
  //       for k:=j+1 to i do
  //     end;
  // end;
  // close(f);
end.