var f:text;
xy:array[1..1000,1..1000] of integer;
o:array[1..1000] of integer;
i,j,k,m,n,s,w:integer;
begin // Chua hoan thanh
  // assign(f,'115.inp');
  // reset(f);
  // readln(f,n,m,w,k,s);
  // for i:=1 to n do read(f,o[i]);
  // for i:=1 to m do 
  //   for j:=1 to 2 do read(f,xy[i,j]);
  // close(f);
  // dec(s,o[k]);
  // assign(f,'115.out');
  // rewrite(f);
  // write(f,w,' ');
  // write(f,)
  // close(f);
end.